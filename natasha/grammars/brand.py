from enum import Enum


class Brand(Enum):

    Default = [
        {
            'labels': [
                ('gram', 'LATN'),
                ('is-capitalized', True),
            ],
            'repeatable': True,
        }
    ]
